{
  "name": "hotstuff",
  "private": true,
  "type": "module",
  "description": "Development tooling only. Nothing in here ships: the site is static files under public/, and the Worker is bundled by wrangler at deploy time.",
  "scripts": {
    "types": "tsc --noEmit --project tsconfig.json && tsc --noEmit --project worker/tsconfig.json"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "linkedom": "^0.18.13",
    "typescript": "^7.0.2"
  }
}
