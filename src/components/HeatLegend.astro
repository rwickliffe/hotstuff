---
import { heatWord } from "../../public/lib/data.js";
import HeatPips from "./HeatPips.astro";

const samples = [1, 3, 5, 6];
---
<div class="scale-key">
  {samples.map((n) => (
    <div>
      <HeatPips heat={n} />
      <small>{heatWord(n)}</small>
    </div>
  ))}
</div>
